<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Private Invitation</title>

<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@600&family=Montserrat:wght@300;500&display=swap" rel="stylesheet">

<style>
body {
    margin: 0;
    background: #0d0d0d;
    font-family: 'Montserrat', sans-serif;
    color: #d4af37;
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
    overflow: hidden;
}

.envelope-wrapper {
    perspective: 1000px;
}

.envelope {
    width: 320px;
    height: 200px;
    background: #1a1a1a;
    position: relative;
    cursor: pointer;
    transition: transform 1s;
    border: 1px solid #d4af37;
}

.flap {
    width: 0;
    height: 0;
    border-left: 160px solid transparent;
    border-right: 160px solid transparent;
    border-bottom: 100px solid #d4af37;
    position: absolute;
    top: -100px;
    left: 0;
    transform-origin: bottom;
    transition: transform 1s ease;
}

.letter {
    position: absolute;
    width: 100%;
    height: 100%;
    background: #111;
    top: 0;
    left: 0;
    display: flex;
    justify-content: center;
    align-items: center;
    font-family: 'Playfair Display', serif;
    font-size: 1.2em;
    letter-spacing: 2px;
}

.hidden-content {
    position: absolute;
    width: 90%;
    max-width: 600px;
    text-align: center;
    opacity: 0;
    transition: opacity 1.5s ease;
}

h1 {
    font-family: 'Playfair Display', serif;
    font-size: 2.2em;
    margin-bottom: 10px;
}

.details {
    margin-top: 20px;
    line-height: 1.8em;
    font-size: 1.1em;
}

.countdown {
    margin-top: 30px;
    font-size: 1.2em;
    letter-spacing: 2px;
}

.open .flap {
    transform: rotateX(-180deg);
}

.open .letter {
    display: none;
}

.opened-content {
    opacity: 1;
}
</style>
</head>

<body>

<div class="envelope-wrapper" onclick="openEnvelope()">
    <div class="envelope" id="envelope">
        <div class="flap"></div>
        <div class="letter">
            OPEN YOUR INVITATION
        </div>
    </div>
</div>

<div class="hidden-content" id="content">
    <h1>IT'S OUR SECOND VALENTINE'S DAY TOGETHER</h1>
    <p style="font-weight:300;">And I don’t do ordinary nights.</p>

    <div class="details">
        Valentine’s Day Dinner Invitation<br><br>
        📅 12 February 2026<br>
        📍 Il Mare, Hotel Mulia<br>
        ⏰ 7:00 PM<br>
        👗 Dress how you like…<br>
        I’ll still be the luckiest man in the room.
    </div>

    <div class="countdown" id="countdown"></div>
</div>

<script>
function openEnvelope() {
    document.getElementById("envelope").classList.add("open");
    setTimeout(() => {
        document.getElementById("content").classList.add("opened-content");
    }, 1000);
}

// Countdown Timer
const targetDate = new Date("February 12, 2025 19:00:00").getTime();

const countdownFunction = setInterval(function() {
    const now = new Date().getTime();
    const distance = targetDate - now;

    if (distance < 0) {
        document.getElementById("countdown").innerHTML = "Tonight.";
        clearInterval(countdownFunction);
        return;
    }

    const days = Math.floor(distance / (1000 * 60 * 60 * 24));
    const hours = Math.floor((distance % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
    const minutes = Math.floor((distance % (1000 * 60 * 60)) / (1000 * 60));
    const seconds = Math.floor((distance % (1000 * 60)) / 1000);

    document.getElementById("countdown").innerHTML =
        days + " DAYS • " +
        hours + " HOURS • " +
        minutes + " MIN • " +
        seconds + " SEC";
}, 1000);
</script>

</body>
</html>
